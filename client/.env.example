# Copy to client/.env. Create React App only exposes variables prefixed REACT_APP_.
# API calls are proxied to http://localhost:3001 via the "proxy" field in client/package.json.

# Base URL for static images served from client/public/images/ (needs the trailing slash)
REACT_APP_PUBLIC_FOLDER=http://localhost:3000/images/
