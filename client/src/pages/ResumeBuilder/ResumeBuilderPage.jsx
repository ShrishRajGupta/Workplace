import Body from "./components/Body/Body";

// The resume builder keeps its own component tree and CSS modules under ./components.
const ResumeBuilderPage = () => (
  <div className="resumeBuilder">
    <Body />
  </div>
);

export default ResumeBuilderPage;
