# Copy to socket/.env

# Port the socket.io server listens on (the client connects to ws://localhost:PORT)
PORT=8900

# Origin allowed to connect (the CRA dev server)
CLIENT_URL=http://localhost:3000

# Must match the API server's ACCESS_TOKEN: the socket server verifies the same auth cookie
ACCESS_TOKEN=
